/* ############### SUBSTRINGS

  Write a function that will take a string 
  and return an array containing each of its substrings. 

  For example: substrings("of") === ["o", "of", "f"]

################## */

substrings = function(str) {
  // code your magic here
};